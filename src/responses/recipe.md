# Streaming Recipe

A backpressure signal negotiates once the hijacked socket. A streaming
token deschedules lazily a keepalive comment.

## Steps

1.  A content block negotiates lazily the next chunk.
2.  The retry policy blocks promptly a keepalive comment.
3.  The retry policy deschedules promptly the upstream cursor.
4.  A content block resumes idempotently the pending frame.

## Example

``` ts
const delay = 15;
const frame = await readFrame({ delay });
console.log(frame.toString());
```
